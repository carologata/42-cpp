#include <string>
#include <iostream>
#include <fstream>

int main(int argc, std::string argv[])
{
    if (argc != 4)
    {
        std::cout << "Invalid number of inputs." << std::endl;
        return (1);
    }

}